{
  "name": "desafio-chat2desk",
  "version": "1.0.0",
  "description": "API cadastro e login de usuários",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/server.ts"
  },
  "author": "Junior Paula",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.14",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.5"
  }
}
